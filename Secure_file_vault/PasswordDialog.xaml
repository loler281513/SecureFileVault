<Window x:Class="Secure_file_vault.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Secure_file_vault"
        mc:Ignorable="d"
        Title="Ввод пароля" Height="200" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="MessageText" Text="Введите пароль" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <PasswordBox Grid.Row="1" x:Name="PasswordBox" Height="25" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Подтверждение пароля:" Margin="0,0,0,5" 
                   x:Name="ConfirmLabel" Visibility="Collapsed"/>

        <PasswordBox Grid.Row="3" x:Name="ConfirmPasswordBox" Height="25" Margin="0,0,0,10"
                     Visibility="Collapsed"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Click="OkButton_Click" Margin="0,0,5,0"/>
            <Button Content="Отмена" Width="80" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>